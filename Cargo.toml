[package]
name = "puntos_atraccion"
version = "0.1.0"
edition = "2021"

[dependencies]
macroquad = "0.4"
egui-macroquad = { version = "0.17", default-features = false }
rand = "0.8"
rayon = "1"

[profile.release]
opt-level = 3
lto = true
